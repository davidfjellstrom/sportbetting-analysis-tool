# Runtime dependencies of the API on Vercel, and nothing else. The Streamlit
# app, the test suite and the not-yet-written stats modules install through
# pyproject.toml extras; none of them belongs in the function bundle.
#
# `-e .` installs src/ as the same flat modules the tests and the Streamlit
# app import, so `import loader` resolves the same way everywhere. Its
# `[project] dependencies` are pandas and numpy only.
-e .
fastapi>=0.141.1
python-multipart>=0.0.20
